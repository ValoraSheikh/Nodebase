import { Button } from "@/components/ui/button";
import React from "react";

const page = () => {
  return <h1 className="min-h-screen flex items-center justify-center">
    <Button variant="link">Pick Me!</Button>
  </h1>;
};

export default page;
